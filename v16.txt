diff --git a/inventory.html b/inventory.html
index a8c8f46..dad1ccc 100644
--- a/inventory.html
+++ b/inventory.html
@@ -166,7 +166,7 @@
 <input id="labPick" type="file" accept="image/*" style="display:none">
 <div id="modal"><div class="sheet" id="sheet"></div></div>
 <div id="err" class="err"></div>
-<footer>inventory v15</footer>
+<footer>inventory v16</footer>
 
 <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
 <script>
@@ -566,10 +566,12 @@ function renderExDetail(){
     +'<div class="muted" style="font-size:13px;margin-bottom:8px">'+esc(p.title||"—")+'</div>'
     +'</div>';
   const gate=cons&&cons.verdict==="consensus";
-  if(!gate){
-    body+='<label class="toggle"><input type="checkbox" id="xOverride"'+(exOverride?" checked":"")+'> '
-      +'override — I read the number off the screenshot myself</label>';
-  }
+  // E5 (dispatch 107-REM-D): render the override checkbox UNCONDITIONALLY
+  // (was if(!gate)) so it is reachable even on a consensus row; gate is
+  // still used by the button face and promoteEx, and exOverride stays
+  // default false, reset on open/back.
+  body+='<label class="toggle"><input type="checkbox" id="xOverride"'+(exOverride?" checked":"")+'> '
+    +'override — I read the number off the screenshot myself</label>';
   body+='<button data-ex-act="promote"'+((gate||exOverride)?"":' class="ghost"')+'>'
     +'Confirm &amp; promote to unit'+((gate||exOverride)?"":" (blocked)")+'</button>';
   if(exNote)body+='<div class="ok" style="display:block">'+esc(exNote)+'</div>';
